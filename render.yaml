services:
  - type: web
    name: blackjack-game
    env: java
    buildCommand: mvn clean package -DskipTests
    startCommand: java -jar target/gambling-0.0.1-SNAPSHOT.jar
    envVars:
      - key: JAVA_VERSION
        value: 17
